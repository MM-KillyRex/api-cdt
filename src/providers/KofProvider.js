
    

    //titles.forEach()

    
    /* 
    let acum = 0;
    let valuesT = titles[acum].titulo;
    console.log(valuesT);

    // asignamos el valor de la celda A! de 100 como numero y el estilo asignado
    ws.cell(1,1) //C: A F: 1
    .number(100)
    .style(style);

    // Set value of cell B1 to 200 as a number type styled with paramaters of style
    ws.cell(1, 2)
    .number(200)
    .style(style);

    // Set value of cell C1 to a formula styled with paramaters of style
    ws.cell(1, 3)
    .formula('A1 + B1')
    .style(style);

    // Set value of cell A2 to 'string' styled with paramaters of style
    ws.cell(2, 1)
    .string('string')
    .style(style);

    // Set value of cell A3 to true as a boolean type styled with paramaters of style but with an adjustment to the font size.
    ws.cell(3, 1)
    .bool(true)
    .style(style)
    .style({font: {size: 14}});

    

    
    / obtener las fecha de hoy
    let date = DateTime.now ();    
    // Agrega 100 horas a la fecha    
    fecha = fecha.addHours (100);    
    // Escriba la fecha de forma formateada: lunes 15 / febrero / 2012    
    document.writeline ( "dddd, dd / MMM / aaaa" );
    */ 